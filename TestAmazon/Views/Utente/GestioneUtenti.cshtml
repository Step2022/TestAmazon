@model IEnumerable<TestAmazon.Models.Utente>

@{
    ViewBag.Title = "GestioneUtenti";
}

<h2>GestioneUtenti</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<p style="color:red">@ViewBag.Success</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Nome)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Cognome)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Email)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Password)
        </th>
        <th>
            @Html.DisplayName("Ruolo")
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Nome)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Cognome)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Email)
        </td>
        <td>
            ********
            @*@Html.DisplayFor(modelItem => item.Password)*@
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Ruolo.Nome_ruolo)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id = item.Id_Utente }) |
            @Html.ActionLink("Details", "Details", new { id = item.Id_Utente }) |
            @Html.ActionLink("Elimina", "Delete", "Utente", new
       {
           @class = "btn btn-warning",
           onclick = string.Format("return confirm('WARNING: Delete {0} {1}?');", item.Nome,item.Cognome)
       })
           
          @* new  { onclick = $"\"return confirm('Procedere con l'eliminazione di  {@item.Nome} {@item.Cognome} ? ');\"" })*@


        @*new { onclick = "return confirm('Procedere con l'eliminazione di ')" })*@
        </td>
    </tr>
}

</table>

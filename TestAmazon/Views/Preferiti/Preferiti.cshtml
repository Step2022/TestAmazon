@model IEnumerable<TestAmazon.Models.Preferiti>

@{
    ViewBag.Title = "Preferiti";
}

<h2>Preferiti</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Id_Preferiti)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Prodotto.Nome)
        </th>
        <th>

            @Html.DisplayNameFor(model => model.Utente.Nome)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Id_Preferiti)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Prodotto.Nome)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Utente.Nome)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { /* id=item.PrimaryKey */ }) |
            @Html.ActionLink("Details", "Details", new { /* id=item.PrimaryKey */ }) |
            @Html.ActionLink("Delete", "Delete", new { /* id=item.PrimaryKey */ })
        </td>
    </tr>
}

</table>

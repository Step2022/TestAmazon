
<!DOCTYPE html>
<html>
<head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>@ViewBag.Title - My ASP.NET Application</title>
    @using TestAmazon.Utility
    @Styles.Render("~/Content/css")
    @Scripts.Render("~/bundles/modernizr")
    @{
        string nameUser = null;
        long idUser = 0;
        int idRuolo = 0;
        if (Session["Username"] != null)
        {
            nameUser = Session["Username"].ToString();
        }
        if (Session["IdUser"] != null && RegularExp.IsInt(Session["IdUser"].ToString()))
        {
            idUser = int.Parse(Session["IdUser"].ToString());
        }
        if (Session["IdRuolo"] != null && RegularExp.IsInt(Session["IdRuolo"].ToString()))
        {
            idRuolo = int.Parse(Session["IdRuolo"].ToString());
        }
    }
   
</head>
<body>
    <div class="navbar navbar-inverse navbar-fixed-top">
        <div class="row">
            <div class="col-lg-9">
                <a href="/"><img src="~/IMGS/logo.png" /></a>
            </div>
            <div class="col-lg-9">
                @*sezione login*@
            </div>
        </div>
        @*<div class="container">
            <div class="navbar-header">
                <button type="button" class="navbar-toggle" data-toggle="collapse" data-target=".navbar-collapse">
                    <span class="icon-bar"></span>
                    <span class="icon-bar"></span>
                    <span class="icon-bar"></span>
                </button>
                @Html.ActionLink("Application name", "Index", "Home", new { area = "" }, new { @class = "navbar-brand" })
            </div>
            <div class="navbar-collapse collapse">
                <ul class="nav navbar-nav">
                    <li>@Html.ActionLink("Home", "Index", "Home")</li>
                    <li>@Html.ActionLink("About", "About", "Home")</li>
                    <li>@Html.ActionLink("Contact", "Contact", "Home")</li>


                    @if (@Session["Utente"] != null)
                    {
                        if (@Session["Ruolo"].ToString() == "admin")
                        {
                            <li>@Html.ActionLink("Gestione Utenti", "GestioneUtenti", "Utente")</li>
                        }
                        <li style="color:white"><a onclick="return false">Ciao @Session["Utente"].ToString()</a></li>
                        <li>@Html.ActionLink("Logout", "Logout", "Utente")</li>
                    }
                    else
                    {
                        <li>@Html.ActionLink("Sign-Up", "SignUp", "Utente")</li>
                        <li>@Html.ActionLink("Login", "Login", "Utente")</li>
                    }


                </ul>
            </div>
        </div>*@
    </div>
    <div class="container body-content">
        @RenderBody()
        <hr />
        <footer>
            <p>&copy; @DateTime.Now.Year - My ASP.NET Application</p>
        </footer>
    </div>

    @Scripts.Render("~/bundles/jquery")
    @Scripts.Render("~/bundles/bootstrap")
    @RenderSection("scripts", required: false)
</body>
</html>
